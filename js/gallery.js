document.querySelectorAll('img.image').forEach((item) => {
    item.addEventListener('click', (event) => {
        let image = event.target.getAttribute('src');
        document.getElementById('modal').style
    })
});